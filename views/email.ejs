<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email</title>
</head>
<body>
    <h1>Email</h1>
    <% for(let i = 0 ; i < data.value.length ; i++){ %>
        <p><a href="<%= data.value[i].webLink %>"><%= data.value[i].webLink %></a></p>
    <% } %>
</body>
</html>